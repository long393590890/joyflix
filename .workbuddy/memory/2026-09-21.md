# 2026-09-21

## JoyFlix 资源源分类比对
- 用户带来一批 90 条的新资源站列表（MoonTV 风格导出，含 config/custom 两类来源），要求与项目 config.json 按 API 地址完全一致标准分类。
- 用脚本精确比对（脚本与数据存于 `.workbuddy/tmp/compare_sources.js`、`new_sources.json`）：
  - 已有（地址完全一致）：32 条，对应 config.json 中 21 个 key
  - 新增：58 条，去重后 40 个唯一地址；其中 4 个仅 http/https 协议差异、4 个为 pz.v88.qzz.io 代理包装源
  - 该列表内部 config/custom 两部分存在大量同地址重复
- 项目背景：JoyFlix 播放源来自 config.json 的 api_site（苹果 CMS V10 采集站），downstream.ts 搜索取 m3u8，播放页做优选测速。
- 后续可能需求：将新增源合入 config.json。

## 新增源已合入 config.json
- 在 config.json 的 api_site 末尾追加了 36 个新源（27→63 个站点），key 全部小写无冲突：ffzy2、wujin2/3/4、zy360zz、yyzy、zy1080、shenma、uku2、ckzy、xbzy、xinlang、nnzy、xsdzy、hhzy、hn3、sb2、sdzy、plzy、lzi2、modu、zuidazy、iqiyi、yzzy、dbzy2/3、hhzyproxy、aidan、ruyiproxy、bdzyproxy、wolong1/2/wolong、wwzy2、jinying、jy2。
- 未合入 4 个仅 http/https 协议差异的重复项；未携带原列表的 detail 字段（本项目 detail 会强制走爬虫路径，保守起见仅 api+name）。
- 已跑 scripts/convert-config.js 重新生成 src/lib/runtime.ts（63 条）。
- 遗留：config 原有 yinghua 与 yh 两个 key 指向同一地址（历史遗留，未动，删除会影响存量播放记录）。
